export default function getMultiLineOperationResult(value: string, result: number | null): number {
    const stack = result !== null ? [result] : [];

    // is the line a number
    // is the line an operator
    // is the line a number and an operator

  return 0
}